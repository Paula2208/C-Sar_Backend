const {encryptEspagnol} = require('../src/handlers/encryptEspagnol');
const {encryptEnglish} = require('../src/handlers/encryptEnglish');

const encryptEsp = (req , res) => {

};

const encryptEng = (req , res) => {

};

module.exports = {encryptEsp, encryptEng};